import styled from "styled-components";

export const StyledHeroTagline = styled.section`
  .tagline {
    color: #fdf6ee;
    font-size: 5rem;
    font-family: "Montserrat", sans-serif;
    font-weight: 700;
    text-align: center;
    cursor: pointer;
    max-width: fit-content;
    margin: 0 auto;
  }

  /* Refresh Button Style */
  /* .refresh-btn {
    width: 4rem;
    display: block;
    margin: 0 auto;
    padding: 2rem;
    cursor: pointer;
  } */
`;
